---
- name: Configure backup and OADP
  hosts: localhost
  gather_facts: false
  roles:
    - role: day2_operational_workflow
      vars: {day2_operation_domain: backup-oadp, day2_operation_resources: "{{ backup_oadp_resources | default([]) }}"}
