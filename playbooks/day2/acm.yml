---
- import_playbook: platform-operator-bundle.yml
  vars:
    platform_operator_components:
      - acm
    platform_operator_resources: "{{ acm_resources | default([]) }}"
    platform_operator_render_root: "{{ playbook_dir }}/../../rendered/day2/platform-operators/acm"
