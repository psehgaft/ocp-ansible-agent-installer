---
- name: Configure Red Hat Advanced Cluster Security policy resources
  hosts: localhost
  gather_facts: false
  roles:
    - role: day2_operational_workflow
      vars:
        day2_operation_domain: rhacs-policies
        day2_operation_resources: "{{ rhacs_policy_resources | default([]) }}"
