---
- name: Configure NMState policies
  hosts: localhost
  gather_facts: false
  roles:
    - role: day2_cluster_operations
      vars: {day2_cluster_domain: nmstate, day2_cluster_resources: "{{ nmstate_resources | default([]) }}"}
