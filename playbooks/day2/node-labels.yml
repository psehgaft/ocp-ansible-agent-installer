---
# Required input contract: node_label_assignments and day2_deployment_mode.
- name: Apply validated OpenShift node labels
  hosts: localhost
  gather_facts: false
  roles:
    - role: day2_node_labels
