# Tests

Repository-wide validation belongs here. Component-specific integration tests should remain close to their roles or Molecule scenarios.

Planned checks include YAML linting, Ansible linting, playbook syntax checks, manifest rendering, kubeconform validation, and focused Python tests for custom plugins.
