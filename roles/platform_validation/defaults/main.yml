---
platform_validation_allowed_modes:
  - direct
  - render
  - gitops
platform_validation_supported_versions:
  - "4.18"
  - "4.19"
platform_validation_require_kubeconfig: false
platform_validation_kubeconfig: "{{ lookup('env', 'KUBECONFIG') | default('', true) }}"
